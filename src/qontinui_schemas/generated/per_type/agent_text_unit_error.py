# generated by datamodel-codegen:
#   filename:  AgentTextUnitError.json
#   timestamp: 2026-08-24T21:14:23+00:00

from __future__ import annotations

from typing import Annotated, Literal

from pydantic import BaseModel, Field, RootModel


class AgentTextUnitError1(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    name: str
    reason: Literal['invalid-name']


class AgentTextUnitError2(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    name: str
    reason: Literal['reserved-name']


class AgentTextUnitError3(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    kind: str
    reason: Literal['invalid-kind']


class AgentTextUnitError4(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    path: str
    reason: Literal['invalid-file-path']


class AgentTextUnitError5(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    path: str
    reason: Literal['reserved-file-path']


class AgentTextUnitError6(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    reason: Literal['empty-file-set']


class AgentTextUnitError7(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    count: Annotated[int, Field(ge=0)]
    max: Annotated[int, Field(ge=0)]
    reason: Literal['too-many-files']


class AgentTextUnitError8(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    path: str
    reason: Literal['blank-file']


class AgentTextUnitError9(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    bytes: Annotated[int, Field(ge=0)]
    max: Annotated[int, Field(ge=0)]
    path: str
    reason: Literal['file-too-large']


class AgentTextUnitError10(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    bytes: Annotated[int, Field(ge=0)]
    max: Annotated[int, Field(ge=0)]
    reason: Literal['unit-too-large']


class AgentTextUnitError11(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    entrypoint: str
    kind: str
    name: str
    reason: Literal['missing-entrypoint']


class AgentTextUnitError12(BaseModel):
    """
    Failure modes for agent text units. Mirrors the tagged-enum shape of
    [`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],
    so the qontinui-web layer surfaces a rejected unit the same way it surfaces
    a rejected app registration.

    One variant per raise site in qontinui-web's `AgentTextUnitValidationError`,
    so a Rust caller can render exactly the refusal the store would give.
    """

    name: str
    reason: Literal['underscore-not-invocable']


class AgentTextUnitError(
    RootModel[
        AgentTextUnitError1
        | AgentTextUnitError2
        | AgentTextUnitError3
        | AgentTextUnitError4
        | AgentTextUnitError5
        | AgentTextUnitError6
        | AgentTextUnitError7
        | AgentTextUnitError8
        | AgentTextUnitError9
        | AgentTextUnitError10
        | AgentTextUnitError11
        | AgentTextUnitError12
    ]
):
    root: Annotated[
        AgentTextUnitError1
        | AgentTextUnitError2
        | AgentTextUnitError3
        | AgentTextUnitError4
        | AgentTextUnitError5
        | AgentTextUnitError6
        | AgentTextUnitError7
        | AgentTextUnitError8
        | AgentTextUnitError9
        | AgentTextUnitError10
        | AgentTextUnitError11
        | AgentTextUnitError12,
        Field(
            description="Failure modes for agent text units. Mirrors the tagged-enum shape of\n[`crate::apps::AppError`] and [`crate::agent_commands::AgentCommandError`],\nso the qontinui-web layer surfaces a rejected unit the same way it surfaces\na rejected app registration.\n\nOne variant per raise site in qontinui-web's `AgentTextUnitValidationError`,\nso a Rust caller can render exactly the refusal the store would give.",
            discriminator='reason',
            title='AgentTextUnitError',
        ),
    ]
