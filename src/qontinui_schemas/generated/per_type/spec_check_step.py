# generated by datamodel-codegen:
#   filename:  SpecCheckStep.json
#   timestamp: 2026-09-24T22:29:48+00:00

from __future__ import annotations

from enum import StrEnum
from typing import Annotated, Any

from pydantic import BaseModel, ConfigDict, Field


class RetrySpec(BaseModel):
    """
    Per-step retry configuration carried by [`BaseStepFields::retry`].

    Distinct from the workflow frame's `RetryPolicy` — that one also carries a
    `backoff` flag, this per-step form is the older, simpler shape that step
    DTOs inherited from the TS `BaseStep` interface.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    count: Annotated[
        int, Field(description='Number of retry attempts (`0` = no retries).', ge=0)
    ]
    delayMs: Annotated[
        int, Field(description='Delay between retries in milliseconds.', ge=0)
    ]


class VerificationCategoryKind(StrEnum):
    """
    Verification-depth category for a step.

    Mirrors the TS `VerificationCategory` literal union. Kept local to this
    module because it is only referenced from [`BaseStepFields`].
    """

    existence = 'existence'
    uniqueness = 'uniqueness'
    referential_integrity = 'referential_integrity'
    semantic_correctness = 'semantic_correctness'
    runtime_behavior = 'runtime_behavior'


class SpecCheckStep(BaseModel):
    """
    Evaluate a page spec against the live UI of a registered app.

    Wire tag: `"spec_check"`.

    Not [`crate::spec_check::SpecCheckStepConfig`]: that is the camelCase,
    `deny_unknown_fields` policy config with no app id. This is the step as the
    workflow generator and the runner's `ExecutionStepConfig` carry it.
    """

    criterionIds: Annotated[
        list[str] | None,
        Field(description='Acceptance criterion IDs verified by this step.'),
    ] = None
    dependsOn: Annotated[
        list[str] | None,
        Field(description='IDs of other steps that must complete first.'),
    ] = None
    extract: Annotated[
        dict[str, str] | None,
        Field(description='Extractions published to subsequent steps.'),
    ] = None
    failOnConsoleErrors: Annotated[
        bool | None,
        Field(
            description='If `Some(true)`, a console-error signal from the UI fails this step.'
        ),
    ] = None
    id: Annotated[
        str | None,
        Field(
            description='Unique identifier for the step. Absent or `null` reads as `""`.'
        ),
    ] = ''
    inputs: Annotated[
        dict[str, str] | None,
        Field(description='Named input bindings evaluated at step entry.'),
    ] = None
    name: Annotated[
        str | None,
        Field(description='Display name for the step. Absent or `null` reads as `""`.'),
    ] = ''
    required: Annotated[
        bool | None,
        Field(
            description='Whether this step is required (default: `true` on consumer side).'
        ),
    ] = None
    retry: Annotated[
        RetrySpec | None, Field(description='Per-step retry configuration.')
    ] = None
    skillOrigin: Annotated[
        Any | None,
        Field(
            description='Provenance of this step when generated from a skill template.\n\nTyped as `serde_json::Value` here to avoid pulling the `skill`\ndependency chain into this module; the TS side re-imports the typed\n`SkillOrigin` after regeneration.'
        ),
    ] = None
    specCheckAppId: Annotated[
        str | None,
        Field(
            description='App id whose specs root resolves `spec_check_page_id`. Required at run\ntime (the handler refuses a missing one); optional on the wire.'
        ),
    ] = None
    specCheckFailOn: Annotated[
        list[str] | None,
        Field(
            description='Snapshot-fetch error kinds that fail the step (lower snake_case, e.g.\n`"not_connected"`, `"timeout"`).'
        ),
    ] = None
    specCheckFailWhenNoApp: Annotated[
        bool | None,
        Field(
            description='Fail the step when the app is unreachable (default `true` on the\nconsumer side).'
        ),
    ] = None
    specCheckFailWhenNoSpec: Annotated[
        bool | None,
        Field(
            description='Fail the step when the page has no spec (default `true` on the\nconsumer side).'
        ),
    ] = None
    specCheckPageId: Annotated[
        str | None, Field(description='Page id whose spec is evaluated.')
    ] = None
    specCheckPolicy: Annotated[
        Any | None,
        Field(
            description='AND-conjunct policy, carried verbatim; the handler reconstitutes the\ntyped policy.'
        ),
    ] = None
    verificationCategory: Annotated[
        VerificationCategoryKind | None,
        Field(description='Verification depth category.'),
    ] = None
